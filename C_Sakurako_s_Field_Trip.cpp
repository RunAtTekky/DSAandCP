#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n; cin >> n;

    vector<int> topics(n);
    for (auto &topic : topics) cin >> topic;

    int minimum_disturbance = 0;

    cout << minimum_disturbance << "\n";
}

int main() {
    int t=1; cin >> t;
    while (t--) solve();
}
