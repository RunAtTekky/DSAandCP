#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

void solve() {
    string score; cin >> score;

    char winner = score[score.size() - 2];

    cout << winner << "\n";
}

int main() {
ios_base::sync_with_stdio(false); cin.tie(NULL);
    int t=1;
    while (t--) solve();
}

